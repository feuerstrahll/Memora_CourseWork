# 📚 Архивное веб-приложение

Full-stack веб-приложение для управления архивными фондами с аутентификацией, разграничением прав доступа и расширенным поиском.

## ✨ Основные возможности

- 🔐 **Аутентификация** — JWT токены, 3 роли (Администратор, Архивариус, Исследователь)
- 📁 **Управление фондами** — CRUD для фондов, описей и единиц хранения
- 📎 **Загрузка документов** — PDF/DOCX файлы до 10MB
- 🔍 **Расширенный поиск** — фильтрация и сортировка по столбцам таблицы
- 📊 **Статистика** — визуализация с графиками (Recharts)
- 📝 **Система заявок** — подача заявок на просмотр/оцифровку документов
- 👤 **Личный профиль** — редактирование имени, рода деятельности, места работы
- 👥 **Управление пользователями** — назначение ролей (только админ)

---

## 🛠 Технологии

| Frontend | Backend | Инфраструктура |
|----------|---------|----------------|
| React 18 + TypeScript | NestJS + TypeScript | Docker + Docker Compose |
| Vite | TypeORM + PostgreSQL 15 | Monorepo структура |
| TanStack Query | JWT + Passport | |
| React Router | class-validator + Multer | |
| Recharts | Swagger | |

---

## 🚀 Быстрый старт

```bash
# 1. Запуск
docker-compose up -d

# 2. Дождитесь запуска (~30 сек)
docker-compose logs -f backend

# 3. Откройте http://localhost:5173
```

### Доступные сервисы:
- 🌐 **Frontend**: http://localhost:5173
- 🔌 **Backend API**: http://localhost:3000
- 📖 **Swagger**: http://localhost:3000/api/docs

---

## 👤 Первая настройка

1. Зарегистрируйтесь на `/register`
2. Назначьте себе роль админа:
```bash
docker exec -it archive_postgres psql -U archive_user -d archive_db
UPDATE users SET role = 'admin' WHERE email = 'ваш@email.com';
\q
```
3. Перелогиньтесь

---

## 👥 Роли пользователей

| Роль | Права |
|------|-------|
| 🔍 **Исследователь** | Поиск, просмотр, заявки, редактирование профиля (род деятельности, место работы, должность) |
| 📋 **Архивариус** | + CRUD данных, обработка заявок |
| 👑 **Администратор** | + управление пользователями |

---

## 📎 Работа с файлами

- **Форматы**: PDF, DOCX (до 10 МБ)
- **Хранение**: `backend/uploads/records/`
- **Доступ для исследователей**: через одобренные заявки
- **Доступ для архивариусов/админов**: напрямую из таблицы

---

## 📂 Структура проекта

```
├── backend/                # NestJS API
│   └── src/
│       ├── auth/          # JWT, RBAC
│       ├── users/         # Пользователи + профиль
│       ├── fonds/         # Фонды
│       ├── inventories/   # Описи
│       ├── records/       # Единицы хранения
│       ├── requests/      # Заявки
│       └── stats/         # Статистика
│
├── frontend/              # React SPA
│   └── src/
│       ├── api/          # API клиенты
│       ├── components/   # Компоненты
│       ├── contexts/     # Auth, Toast
│       └── pages/        # Страницы
│
└── docker-compose.yml
```

---

## 🗄 База данных

Таблицы создаются автоматически через TypeORM.

```
Fond 1→N Inventory 1→N Record
Record N↔M Keyword
Record 1→N Request
User 1→N Request
```

---

## 🔌 API Endpoints

| Метод | Endpoint | Описание |
|-------|----------|----------|
| POST | `/api/auth/register` | Регистрация |
| POST | `/api/auth/login` | Вход |
| GET | `/api/records/search` | Поиск с фильтрами |
| POST | `/api/records/:id/upload` | Загрузка файла |
| GET | `/api/records/:id/download` | Скачивание файла |
| GET/PATCH | `/api/users/profile` | Профиль пользователя |
| POST | `/api/requests` | Создание заявки |
| PATCH | `/api/requests/:id` | Изменение статуса |

📖 Полная документация: http://localhost:3000/api/docs

---

## 🎨 Страницы

| Маршрут | Описание | Доступ |
|---------|----------|--------|
| `/` | Dashboard + личная информация | Все |
| `/records` | Единицы хранения (сортировка, фильтры) | Все |
| `/fonds`, `/inventories` | Управление данными | Archivist+ |
| `/requests` | Заявки | Все |
| `/stats` | Графики статистики | Все |
| `/users` | Управление пользователями | Admin |

---

## 🔍 Поиск и фильтрация

- **Текстовый поиск** — по названию, аннотации, шифру
- **Фильтры по столбцам** — шифр, название, опись, фонд, доступ, наличие файла
- **Сортировка** — по любому столбцу (↑↓)
- **Фильтр по фонду/описи** — выпадающие списки
- **Пагинация** — постраничный вывод

---

## 🔐 Безопасность

- JWT токены с истечением срока
- RBAC на уровне роутов
- Bcrypt хеширование паролей
- Валидация всех входных данных
- Защита от SQL-инъекций (TypeORM)
- CORS настройка

---

## 🧪 Разработка

```bash
# Логи
docker-compose logs -f

# Перезапуск
docker-compose restart backend frontend

# Пересборка
docker-compose up --build

# Подключение к БД
docker exec -it archive_postgres psql -U archive_user -d archive_db
```

---

## ✅ Соответствие требованиям

- ✅ Node.js + React + PostgreSQL + REST API
- ✅ Регистрация/авторизация с JWT
- ✅ Таблицы с пагинацией
- ✅ CRUD операции
- ✅ Поиск с фильтрами
- ✅ Сортировка по столбцам
- ✅ Разделение ролей
- ✅ Редактирование профиля
- ✅ Обработка ошибок

---

**Версия**: 1.0.0  
**Автор**: информация на странице `/about`
